<?php

class RangosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('rangos')->truncate();

		$rangos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('rangos')->insert($rangos);
	}

}
